# ----------------- logging subroutine ----------------
log2 () {
if [ "$DEBUG" ] ; then
echo $@
fi
echo $@ >> logfile
}

# ----------------- end of logging subroutine ----------------

if [ "$1" != "auto" ] ; then
        DEBUG=1
fi
